# --- Python bytecode / caches (recursive: the old root-only rules leaked) ---
*.pyc
*.pyo
__pycache__/

# --- Virtual environments ---
.venv/
venv/
env/
HouckLab_QICK_BFG/

# --- IDE / editor ---
*.iml
.idea/
.ipynb_checkpoints/

# --- Git worktree scratch dirs (never commit these; a stray gitlink got
#     committed here once and had to be removed by hand) ---
.claude/worktrees/
.codex-worktrees/
