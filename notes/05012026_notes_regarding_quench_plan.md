1. We have tunabilities in on the legs using tunable couplers controlled by DC voltage from the Qblox D5a in the Flux_files in the triangle_lattice_quench folder. 
2. The coupling between rungs are fixed coupling.
3. 'Current-readout sequence: a Desq-loadable experiment that, at the end of the dynamics period, applies a global pi/4 rung beam-splitter then triggers the mux readout. Outputs <j_perp(R, t)> per rung.' The angle should be pi/2 right?
