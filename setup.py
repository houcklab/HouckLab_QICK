from setuptools import setup, find_packages

setup(
    name="houcklab-qick",  # Replace with your desired package name
    version="0.1.0",
    packages=find_packages(),
    install_requires=[],  # Add any dependencies if required
)